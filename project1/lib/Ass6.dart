import 'package:flutter/material.dart';

class Ass6 extends StatelessWidget {
  const Ass6({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: Center(
            child: Container(child: Text("demo"), height: 100, width: 100)));
  }
}
